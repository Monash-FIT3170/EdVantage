version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm run deploy
      artifacts:
        baseDirectory: /dist/
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
    appRoot: frontend